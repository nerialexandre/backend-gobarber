// import Appointment from '../models/Appointment';

class AppointmentController {
  async store(req, res) {
    return res.json({ mensage: 'acerto mizeravi' });
  }
}

export default new AppointmentController();
